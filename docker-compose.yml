services:
  app:
    build: .
    ports:
      - "3000:3000" # Maps host port 3000 to container port 3000
    volumes:
      - ./bd-portal.db:/app/bd-portal.db
    environment:
      - NODE_ENV=production
      - APP_PORT=3000 # Internal container port
    restart: unless-stopped
